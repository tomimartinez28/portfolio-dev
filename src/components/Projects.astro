---
const PROJECTS = [
    {
        title: "Projects 1",
        description:
            "Labore nostrud dolor in do anim aliqua non dolore labore. Ad laborum adipisicing nisi laborum. Incididunt reprehenderit mollit cillum minim mollit.",
        link: "#",
        github: "#",
        image: "/projects/svgl.webp",
        tags: ["Django", "Python"],
    },
];
---

{
    PROJECTS.map(({title, description, tags, github, image, link}) => (
        <article class="">
            <h3 class="text-2xl font-semibold text-yellow-300/85 mt-2">{title}</h3>
            <p class="text-lg mb-4 text-pretty">{description}</p>
            <ul class="">
                {tags.map((tag) => (
                    <li>{tag}</li>
                ))}
            </ul>
            <img class="rounded shadow-2xl shadow-white/10" src={image} alt="Captura de pantalla del proyecto">
        </article>
    ))
}
